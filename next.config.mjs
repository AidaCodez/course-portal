/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images:{
    domains:['ap-south-1.graphassets.com']
  }
};

export default nextConfig;
